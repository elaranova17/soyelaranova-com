import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Recursos Digitales · Elara Nova',
  description: 'Recursos que inspiran, guían y transforman.',
}

export default function RecursosPage() {
  return null
}
